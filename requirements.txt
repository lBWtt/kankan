# ============================================================
# 这个文件是干什么的：后端的"购物清单"——列出后端运行需要安装的所有 Python 工具包。
# 它对应产品里的什么功能：不对应单一功能，是整个后端能跑起来的前提。
# 如果它出错了，用户会看到什么现象：开发者装不上环境、服务起不来，
#   线上则是整个 App 无法连接服务器。
# ============================================================
fastapi>=0.110,<1
uvicorn[standard]>=0.29
sqlalchemy>=2.0,<2.1
alembic>=1.13
psycopg2-binary>=2.9
pydantic>=2.5,<3
pydantic-settings>=2.1,<3
redis>=5
python-multipart>=0.0.9
pyjwt>=2.8
httpx>=0.27
boto3>=1.34
anthropic>=0.69
