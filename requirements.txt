# ============================================================
# 这个文件是干什么的：后端的"购物清单"——列出后端运行需要安装的所有 Python 工具包。
# 它对应产品里的什么功能：不对应单一功能，是整个后端能跑起来的前提。
# 如果它出错了，用户会看到什么现象：开发者装不上环境、服务起不来，
#   线上则是整个 App 无法连接服务器。
# ============================================================
# 所有依赖均锁定上下界：下界保证所需 API 已可用，上界锁定到当前主版本，
# 防止上游大版本升级（如 pydantic 3 / fastapi 1）引入破坏性变更导致构建不可复现或供应链风险。
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2
psycopg2-binary>=2.9,<3
redis>=5,<7
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-multipart>=0.0.9,<1
pyjwt>=2.8,<3
httpx>=0.27,<1
boto3>=1.34,<2
anthropic>=0.69,<1
