# 小红书/抖音 内容抓取方案探讨（你选的 C）

> 定位：你要「从小红书/抖音抓真实、吸引人的 AI 内容」。这份文档把**怎么抓、AI 用在哪、独立脚本怎么接进 app、风险怎么控**讲清楚，给你审。

---

## 〇、先直接回你三个问题

**Q1：独立写成一个 py 文件，你审查，再链接到 app？**
✅ **对，就是这个路子。** 抓取 + 富化是**独立脚本**（跟现在的 `curate_github.py` / `curate_posts.py` 一样，不塞进后端服务）。脚本把结果写进数据库的**候选池**（不是直接上线）→ 你在**审核界面/库里过一遍** → 批准的才进 app。所谓「链接到 app」= 脚本和 app **读写同一个 Postgres**，不用改 app 代码。

**Q2：用 DeepSeek 的 API 是不是也可以？**
✅ **可以。** 「AI 富化」那一步（把抓来的乱内容整理成干净的中文结构化卡片）就是**调一个 LLM API**，DeepSeek / Claude 都行——DeepSeek 更便宜、中文好，很适合。后端 `ai_processor` 里换个 API key 和 endpoint 就行。

**Q3：你说过要调 AI 的 API 来「提取」对吧？**
⚠️ **这里要澄清一个关键区分**，不然会搞混：
- **「抓取」**（从小红书/抖音把内容弄下来）= **程序干的**（HTTP/爬虫/浏览器自动化），**不是 AI**。
- **「提取/富化」**（把抓来的乱文本+图，整理成标题/亮点/标签/中文文案）= **AI 干的**（调 DeepSeek/Claude）。

所以 AI 不负责「从小红书弄下来」，AI 负责「把弄下来的东西变好看」。两步分开。

---

## 一、整条链路（端到端）

```
[抓取程序] 小红书/抖音 → 原始内容（文案+图/视频链接）
     ↓ 下载媒体转存本地/OSS（不热链）
[入候选池] 写 candidate 表（状态 ai_collected）+ 去重
     ↓
[AI 富化] 调 DeepSeek/Claude：清洗 → 中文结构化（标题/亮点/标签/content_type）
     ↓  （candidate 状态 ai_processed）
[你审核] 审核界面/库里过一遍——留质量好+合规的        ← 你把关在这
     ↓  （approve）
[发布] 建 Post/Project + 媒体已在本地/OSS → app 里出现
```

**独立脚本 = 前两步（抓取 + 富化）**，你审的是候选池，app 读的是已发布的。

---

## 二、怎么抓小红书/抖音（技术现实，不绕弯）

两家都**没有开放内容 API**，反爬都狠。三条路，从稳到猛：

### 方案 A：半自动（最稳、最合规、推荐先跑）
- **你/运营手动挑**好内容，把**链接**（或直接复制文案）丢给脚本；脚本负责**下载图/视频 + AI 富化 + 入库**。
- 优点：不碰反爬、不踩自动爬虫的 ToS 红线、质量你控；缺点：量靠人。
- **适合冷启动**——先用这个把「呈现管线」跑通，内容立刻变好看。

### 方案 B：开源爬虫框架（半自动化，量能上去）
- 用成熟开源爬虫（如 **MediaCrawler** 这类，基于 Playwright + 登录 Cookie，支持小红书/抖音搜索/详情）。
- 你提供**登录 Cookie** + 关键词（如「AI绘画」「Midjourney」），框架批量拉搜索结果 → 脚本富化入库。
- 优点：能规模化；缺点：**要维护**（平台改接口/风控就得跟着修）、**灰色**（ToS 禁自动抓）、要控频防封号。

### 方案 C：自己逆向接口（最猛、最不推荐）
- 逆向小红书 `x-s/x-t` 签名、抖音 web 风控，自己写请求。
- 优点：最灵活；缺点：**最脆**（签名一变就挂）、维护成本极高、最容易被封 IP/号。**不建议**，除非 A/B 都满足不了。

> 我的建议：**A 起步**（立刻好看、零风险）→ 量不够再上 **B**（开源框架 + 你的 Cookie + 控频）。**C 先不碰。**

---

## 三、AI 富化那步（DeepSeek/Claude）

抓来的原始内容进 LLM，让它输出结构化 JSON：

- 输入：小红书文案（可能带 emoji/话题/口水话）+ 图
- 输出（LLM 填）：`title` / `tagline` / `summary` / `tags` / `content_type`（ai_image/prompt/…）/ 是否值得发（打分）
- 顺带：**中文润色 + 去营销水分**，风格统一成「看看」调性
- **护栏**：原文进 LLM 前做 prompt 注入清洗；单条 token 上限 + 每轮条数上限控成本

DeepSeek 具体接法：OpenAI 兼容接口，`base_url=https://api.deepseek.com`，模型 `deepseek-chat`。后端 `ai_processor` 抽象成「调一个 chat completion」，换 provider 只改配置。

---

## 四、版权 & 风险（必须知道，你来拍板）

- 直接**搬别人的图/文/视频** = 侵权 + 平台 ToS 禁止。**降低风险的做法**：
  - **AI 富化时做「原创化改写」**（内容原创、风格像、**带出处链接 + 原作者名**），不逐字搬文案。
  - 媒体：优先**标注原作者 + 链接**；能生成/替换的用我们自己的图。
  - 加**原作者归属字段**（数据库已有 `original_author_*`），以后可做「认领」。
- 自动爬（方案 B/C）的**账号/IP 风险**：控频、用小号 Cookie、别高频。
- 一句话：**你是老板、你拍板风险**；我技术上都能实现，只把红线标清楚。

---

## 五、建议的最小可行版（MVP，我可以先做这个）

1. 写 `scrape_enrich.py`（独立脚本）：**输入一批链接/内容**（方案 A）→ 下载媒体转存本地 → 调 **DeepSeek** 富化成中文结构化 → 写**候选池**。
2. 你在库里/简易审核界面过一遍 → 批准 → app 里出现。
3. 跑通后，接**方案 B** 的开源爬虫做自动采集（你提供 Cookie + 关键词）。

**先做哪步你定：**
- **① 只做富化管线**（方案 A：你喂链接/内容，我富化入库）——最快见效、零爬虫风险
- **② 富化 + 开源爬虫采集**（方案 B：自动拉小红书/抖音）——量大，但要维护 + 灰色
- **③ 先把 DeepSeek 接进后端 `ai_processor`**（换掉/并存 Claude），再谈抓取

我倾向 **①（先富化管线）→ 跑通 → 再上 ②（自动采集）**。你看怎么走。
